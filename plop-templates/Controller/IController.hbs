import { Request, Response, NextFunction } from "express";

export interface I{{ pascalCase name }}Controller {
  // controller methods here
  // e.x)
  // getAll{{ pascalCase name }}s(req: Request, res: Response, next: NextFunction): Promise<void>;
}
