import { {{ pascalCase name }} } from "@/domain/entities/{{ pascalCase name }}";

export interface I{{ pascalCase name }}Repository {
  // Repository methods
  // save({{ camelCase name }}: {{ pascalCase name }}): Promise<{{ pascalCase name }}>;
  // findById(id: number): Promise<{{ pascalCase name }} | null>;
  // findByEmail(email: string): Promise<{{ pascalCase name }} | null>;
  // delete({{ camelCase name }}Id: number): Promise<void>;
}
