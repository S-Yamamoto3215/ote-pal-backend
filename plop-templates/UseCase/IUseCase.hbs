import { {{ pascalCase name }} } from "@/domain/entities/{{ pascalCase name }}";

export interface I{{ pascalCase name }}UseCase {
  get{{ pascalCase name }}ById({{ camelCase name }}Id: number): Promise<{{ pascalCase name }} | null>;
  create{{ pascalCase name }}(input: Create{{ pascalCase name }}Input): Promise<{{ pascalCase name }}>;
  update{{ pascalCase name }}({{ camelCase name }}Id: number, input: Create{{ pascalCase name }}Input): Promise<{{ pascalCase name }}>;
  delete{{ pascalCase name }}({{ camelCase name }}Id: number): Promise<void>;
}
