import { Router } from "express";

import { {{ pascalCase name }}ControllerFactory } from "@/application/factories/{{ pascalCase name }}/{{ pascalCase name }}ControllerFactory";

export const {{ camelCase name }}Router = Router();
const {{ camelCase name }}Controller = {{ pascalCase name }}ControllerFactory.create();

// After setting up, add the following to src/infrastructure/http/routes/setRoutes.ts
// app.use("/{{ camelCase name }}s", {{ camelCase name }}Router);

// Add routing here e.x) get
// {{ camelCase name }}Router.get("/", (req, res, next) => {
//   {{ camelCase name }}Controller.getAll{{ pascalCase name }}s(req, res, next);
// });
